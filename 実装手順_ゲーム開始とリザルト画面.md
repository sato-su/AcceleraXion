# FPSゲーム - ゲーム開始とリザルト画面の実装手順

## 概要
Startボタンを押すとゲームが開始され、プレイヤーと敵がスポーンし、5分間戦った後にリザルト画面を表示する機能を実装します。

## 必要なブループリント

### 1. ゲームモード（GM_AcceleraXion）の設定

#### 変数の追加
- `GameState` (Enum型) - ゲームの状態を管理（Waiting, Playing, Finished）
- `GameTimeRemaining` (Float型) - 残り時間（秒）
- `PlayerKills` (Integer型) - プレイヤーのキル数
- `PlayerDeaths` (Integer型) - プレイヤーのデス数
- `EnemyKills` (Integer型) - 敵のキル数（参考用）

#### イベントの実装

**BeginPlayイベント**
```
1. GameState を Waiting に設定
2. GameTimeRemaining を 300.0（5分）に設定
3. プレイヤーのスポーンを無効化
```

**StartGame関数（カスタムイベント）**
```
1. GameState を Playing に設定
2. すべてのプレイヤーをスポーン
3. AI_BlueSide を 5体スポーン
4. AI_RedSide を 5体スポーン
5. タイマーを開始（1秒ごとに UpdateGameTimer を呼び出す）
```

**UpdateGameTimer関数（カスタムイベント）**
```
1. GameTimeRemaining から 1.0 を減算
2. GameTimeRemaining が 0 以下になったら：
   - GameState を Finished に設定
   - タイマーを停止
   - すべてのプレイヤーコントローラーに対して ShowResultScreen を呼び出す
   - プレイヤーの入力を無効化
```

**OnPlayerKilled関数（カスタムイベント）**
```
1. PlayerDeaths を 1 増やす
2. 3秒後にプレイヤーをリスポーン
```

**OnEnemyKilled関数（カスタムイベント）**
```
1. PlayerKills を 1 増やす
2. 一定時間後に敵をリスポーン（必要に応じて）
```

---

### 2. プレイヤーコントローラー（PC_AcceleraXion）の設定

#### 変数の追加
- `MainMenuWidget` (Widget型) - メインメニューUIへの参照
- `ResultWidget` (Widget型) - リザルト画面UIへの参照

#### イベントの実装

**BeginPlayイベント**
```
1. MainMenuWidget を作成（WB_Main から）
2. MainMenuWidget を Viewport に追加
3. マウスカーソルを表示
4. 入力をUIモードに設定
```

**StartGame関数（カスタムイベント）**
```
1. MainMenuWidget を Viewport から削除
2. マウスカーソルを非表示
3. 入力をゲームモードに設定
4. ゲームモードの StartGame 関数を呼び出す
```

**ShowResultScreen関数（カスタムイベント）**
```
1. ResultWidget を作成（WB_Result から）
2. ResultWidget を Viewport に追加
3. マウスカーソルを表示
4. 入力をUIモードに設定
5. ゲームモードから統計情報を取得して ResultWidget に渡す
```

---

### 3. メインメニューUI（WB_Main）の設定

#### ウィジェットの構成
- Startボタン（Button）
- タイトルテキスト（Text）

#### StartボタンのOnClickedイベント
```
1. すべてのプレイヤーコントローラーに対して StartGame を呼び出す
   （またはゲームモードの StartGame を直接呼び出す）
```

---

### 4. リザルト画面UI（WB_Result）の新規作成

#### ウィジェットの構成
- タイトルテキスト「リザルト」
- キル数表示（Text）
- デス数表示（Text）
- K/D比表示（Text）
- 再プレイボタン（Button）
- 終了ボタン（Button）

#### 変数の追加
- `Kills` (Integer型) - キル数
- `Deaths` (Integer型) - デス数

#### イベントの実装

**SetResultData関数（カスタムイベント）**
```
パラメータ：
- Kills (Integer)
- Deaths (Integer)

処理：
1. Kills と Deaths を保存
2. キル数テキストを更新
3. デス数テキストを更新
4. K/D比を計算して表示（Deaths が 0 の場合は Kills を表示）
```

**再プレイボタンのOnClickedイベント**
```
1. 現在のレベルをリロード
   （OpenLevel ノードを使用）
```

**終了ボタンのOnClickedイベント**
```
1. ゲームを終了
   （Quit Game ノードを使用）
```

---

### 5. プレイヤー（BP_PlayerBase）の設定

#### イベントの実装

**OnDeathイベント（既存のものに追加）**
```
1. ゲームモードの OnPlayerKilled を呼び出す
```

**OnKillEnemyイベント（既存のものに追加）**
```
1. ゲームモードの OnEnemyKilled を呼び出す
```

---

### 6. AI（BP_AIBase）の設定

#### イベントの実装

**OnDeathイベント（既存のものに追加）**
```
1. ゲームモードの OnEnemyKilled を呼び出す
```

---

## 実装の詳細手順

### ステップ1: ゲームモードの設定

1. **GM_AcceleraXion** を開く
2. 変数を追加：
   - `GameState` (Enum) - 新規Enum「E_GameState」を作成（Waiting, Playing, Finished）
   - `GameTimeRemaining` (Float)
   - `PlayerKills` (Integer)
   - `PlayerDeaths` (Integer)
   - `AICountPerTeam` (Integer) - デフォルト値: 5（1チームあたりのAI数）
3. BeginPlayイベントで初期化
4. StartGame関数を作成
   - AIスポーン関数（SpawnAITeams）を呼び出す
5. SpawnAITeams関数を作成
   - AI_BlueSide を 5体スポーン
   - AI_RedSide を 5体スポーン
6. UpdateGameTimer関数を作成
7. SetTimerByFunctionName で1秒ごとに UpdateGameTimer を呼び出す

### ステップ2: プレイヤーコントローラーの設定

1. **PC_AcceleraXion** を開く
2. BeginPlayでメインメニューを表示
3. StartGame関数を作成
4. ShowResultScreen関数を作成

### ステップ3: メインメニューUIの設定

1. **WB_Main** を開く
2. Startボタンを追加（まだない場合）
3. StartボタンのOnClickedイベントを設定
4. ゲームモードの StartGame を呼び出す

### ステップ4: リザルト画面UIの作成

1. 新しいウィジェットブループリント **WB_Result** を作成
2. UI要素を配置
3. SetResultData関数を実装
4. ボタンのイベントを設定

### ステップ5: プレイヤーとAIの連携

1. **BP_PlayerBase** で死亡時にゲームモードに通知
2. **BP_AIBase** で死亡時にゲームモードに通知

---

## ブループリントノードの使用例

### ゲームモードでタイマーを設定
```
Set Timer by Function Name
- Function Name: "UpdateGameTimer"
- Time: 1.0
- Looping: True
```

### すべてのプレイヤーコントローラーを取得
```
Get All Actors of Class
- Actor Class: PlayerController
```

### レベルをリロード
```
Open Level
- Level Name: 現在のレベル名
```

### ゲームを終了
```
Quit Game
```

### AIをスポーンする
```
方法1: スポーンポイントを使用する場合
1. Get All Actors of Class → Class: BP_BlueSideSpownPoint
2. ForEach Loop で各スポーンポイントに対して
3. Spawn Actor from Class → Class: BP_AIBlueSide, Transform: スポーンポイントの位置

方法2: 直接スポーンする場合
1. For Loop (0 から 4 まで)
2. Spawn Actor from Class → Class: BP_AIBlueSide
3. Set Actor Location → 適切な位置を設定
```

---

## 注意事項

1. **ゲームモードの参照**: プレイヤーコントローラーやプレイヤーからゲームモードを取得するには `Get Game Mode` ノードを使用
2. **ネットワーク対応**: マルチプレイヤー対応が必要な場合は、すべての関数を `Replicated` または `Server` 関数として設定
3. **スポーン制御**: ゲーム開始前はプレイヤーの自動スポーンを無効化する必要があります
4. **UIの表示**: リザルト画面はすべてのプレイヤーに表示されるように実装
5. **AIスポーンポイント**: レベルエディタで `BP_BlueSideSpownPoint` と `BP_RedSideSpawnPoint` を適切な位置に配置してください（最低5個ずつ推奨）
6. **AIスポーン**: `SpawnAITeams` 関数で `BP_AIBlueSide` と `BP_AIRedSide` をそれぞれ5体ずつスポーンします

---

## テスト手順

1. ゲームを起動
2. メインメニューが表示されることを確認
3. Startボタンをクリック
4. プレイヤーと敵がスポーンすることを確認
5. 5分間プレイ
6. 時間が0になったらリザルト画面が表示されることを確認
7. キル数とデス数が正しく表示されることを確認

