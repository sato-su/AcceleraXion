# ブループリント実装の詳細ガイド

## 1. Enum型の作成（E_GameState）

### 手順
1. Content Browser で右クリック → **Blueprint Enum** を選択
2. 名前を `E_GameState` に設定
3. Enum値を追加：
   - `Waiting` (値: 0)
   - `Playing` (値: 1)
   - `Finished` (値: 2)

---

## 2. ゲームモード（GM_AcceleraXion）の実装

### 変数の追加
1. ブループリントエディタで **Variables** タブを開く
2. **+ Variable** をクリックして以下を追加：

| 変数名 | 型 | デフォルト値 | 説明 |
|--------|-----|-------------|------|
| GameState | E_GameState | Waiting | ゲームの状態 |
| GameTimeRemaining | Float | 300.0 | 残り時間（秒） |
| PlayerKills | Integer | 0 | プレイヤーのキル数 |
| PlayerDeaths | Integer | 0 | プレイヤーのデス数 |
| AICountPerTeam | Integer | 5 | 1チームあたりのAI数 |
| BlueSideSpawnPoints | Array of Actor | (空) | BlueSideのスポーンポイント配列 |
| RedSideSpawnPoints | Array of Actor | (空) | RedSideのスポーンポイント配列 |

### BeginPlayイベントの実装

```
[Event BeginPlay]
    ↓
[Set GameState] → Waiting
    ↓
[Set GameTimeRemaining] → 300.0
    ↓
[Get All Actors of Class] → Class: PlayerStart
    ↓
[ForEach Loop]
    ↓
[Set Actor Enabled] → Enabled: False
```

### StartGame関数（カスタムイベント）の実装

**関数の作成:**
1. **Functions** タブで **+ Function** をクリック
2. 関数名を `StartGame` に設定
3. **Compile** をクリック

**実装内容:**
```
[Custom Event StartGame]
    ↓
[Set GameState] → Playing
    ↓
[Get All Actors of Class] → Class: PlayerStart
    ↓
[ForEach Loop]
    ↓
[Set Actor Enabled] → Enabled: True
    ↓
[Call SpawnAITeams]
    ↓
[Set Timer by Function Name]
    - Function Name: "UpdateGameTimer"
    - Time: 1.0
    - Looping: True
```

### SpawnAITeams関数（カスタムイベント）の実装

**関数の作成:**
1. **Functions** タブで **+ Function** をクリック
2. 関数名を `SpawnAITeams` に設定
3. **Compile** をクリック

**実装内容（方法1: スポーンポイントを使用する場合）:**
```
[Custom Event SpawnAITeams]
    ↓
[Get All Actors of Class] → Class: BP_BlueSideSpownPoint
    ↓
[Set BlueSideSpawnPoints] → [Return Value]
    ↓
[For Loop] → First Index: 0, Last Index: 4 (AICountPerTeam - 1)
    ↓
[Array Get] → Array: BlueSideSpawnPoints, Index: [Loop Index] % [Array Length]
    ↓
[Get Actor Transform]
    ↓
[Spawn Actor from Class]
    - Class: BP_AIBlueSide
    - Transform: [Return Value]
    ↓
[Get All Actors of Class] → Class: BP_RedSideSpawnPoint
    ↓
[Set RedSideSpawnPoints] → [Return Value]
    ↓
[For Loop] → First Index: 0, Last Index: 4 (AICountPerTeam - 1)
    ↓
[Array Get] → Array: RedSideSpawnPoints, Index: [Loop Index] % [Array Length]
    ↓
[Get Actor Transform]
    ↓
[Spawn Actor from Class]
    - Class: BP_AIRedSide
    - Transform: [Return Value]
```

**実装内容（方法2: スポーンポイントが少ない場合、ランダムに選択）:**
```
[Custom Event SpawnAITeams]
    ↓
[Get All Actors of Class] → Class: BP_BlueSideSpownPoint
    ↓
[Set BlueSideSpawnPoints] → [Return Value]
    ↓
[For Loop] → First Index: 0, Last Index: 4 (AICountPerTeam - 1)
    ↓
[Random Integer in Range] → Min: 0, Max: [Array Length] - 1
    ↓
[Array Get] → Array: BlueSideSpawnPoints, Index: [Random Integer]
    ↓
[Get Actor Transform]
    ↓
[Spawn Actor from Class]
    - Class: BP_AIBlueSide
    - Transform: [Return Value]
    ↓
[Get All Actors of Class] → Class: BP_RedSideSpawnPoint
    ↓
[Set RedSideSpawnPoints] → [Return Value]
    ↓
[For Loop] → First Index: 0, Last Index: 4 (AICountPerTeam - 1)
    ↓
[Random Integer in Range] → Min: 0, Max: [Array Length] - 1
    ↓
[Array Get] → Array: RedSideSpawnPoints, Index: [Random Integer]
    ↓
[Get Actor Transform]
    ↓
[Spawn Actor from Class]
    - Class: BP_AIRedSide
    - Transform: [Return Value]
```

### UpdateGameTimer関数（カスタムイベント）の実装

**関数の作成:**
1. **Functions** タブで **+ Function** をクリック
2. 関数名を `UpdateGameTimer` に設定
3. **Compile** をクリック

**実装内容:**
```
[Custom Event UpdateGameTimer]
    ↓
[Set GameTimeRemaining] → [GameTimeRemaining] - 1.0
    ↓
[Branch] → Condition: [GameTimeRemaining] <= 0.0
    ├─ True →
    │   [Set GameState] → Finished
    │   ↓
    │   [Clear Timer by Function Name] → Function Name: "UpdateGameTimer"
    │   ↓
    │   [Get All Actors of Class] → Class: PlayerController
    │   ↓
    │   [ForEach Loop]
    │   ↓
    │   [Cast to PC_AcceleraXion]
    │   ↓
    │   [Call ShowResultScreen]
    │   ↓
    │   [Set Input Mode UI Only]
    │   ↓
    │   [Set Show Mouse Cursor] → Show Mouse Cursor: True
    └─ False → (何もしない)
```

### OnPlayerKilled関数（カスタムイベント）の実装

```
[Custom Event OnPlayerKilled]
    ↓
[Set PlayerDeaths] → [PlayerDeaths] + 1
    ↓
[Set Timer] → Time: 3.0
    ↓
[Get All Actors of Class] → Class: PlayerController
    ↓
[Get First Element]
    ↓
[Get Controlled Pawn]
    ↓
[Destroy Actor]
    ↓
[Respawn Player] (または適切なリスポーン処理)
```

### OnEnemyKilled関数（カスタムイベント）の実装

```
[Custom Event OnEnemyKilled]
    ↓
[Set PlayerKills] → [PlayerKills] + 1
```

---

## 2-1. AIスポーンの詳細実装

### スポーンポイントの準備

レベルエディタで以下を配置：
- **BP_BlueSideSpownPoint** を複数配置（最低5個推奨）
- **BP_RedSideSpawnPoint** を複数配置（最低5個推奨）

### SpawnAITeams関数の実装詳細

#### 変数の追加
- `BlueSideSpawnPoints` (Array of Actor) - BlueSideのスポーンポイント配列
- `RedSideSpawnPoints` (Array of Actor) - RedSideのスポーンポイント配列

#### 実装の流れ

**ステップ1: BlueSide AIのスポーン**
```
1. Get All Actors of Class → Class: BP_BlueSideSpownPoint
2. Set BlueSideSpawnPoints → 取得した配列を保存
3. For Loop (0 から AICountPerTeam - 1 まで)
4. 各ループで：
   - スポーンポイント配列から1つ選択（インデックスまたはランダム）
   - そのスポーンポイントのTransformを取得
   - Spawn Actor from Class → BP_AIBlueSide
```

**ステップ2: RedSide AIのスポーン**
```
1. Get All Actors of Class → Class: BP_RedSideSpawnPoint
2. Set RedSideSpawnPoints → 取得した配列を保存
3. For Loop (0 から AICountPerTeam - 1 まで)
4. 各ループで：
   - スポーンポイント配列から1つ選択（インデックスまたはランダム）
   - そのスポーンポイントのTransformを取得
   - Spawn Actor from Class → BP_AIRedSide
```

### ノードの詳細説明

#### Get All Actors of Class
- **場所**: 右クリック → `Get All Actors of Class` を検索
- **Class**: `BP_BlueSideSpownPoint` または `BP_RedSideSpawnPoint` を指定
- **戻り値**: 配列（Array of Actor）

#### For Loop
- **場所**: 右クリック → `For Loop` を検索
- **First Index**: 0
- **Last Index**: `AICountPerTeam - 1` (4)
- **Loop Index**: 現在のループカウンター（0, 1, 2, 3, 4）

#### Array Get
- **場所**: 右クリック → `Array Get` を検索
- **Array**: `BlueSideSpawnPoints` または `RedSideSpawnPoints`
- **Index**: `Loop Index` または `Loop Index % Array Length`（配列が少ない場合の循環）

#### Get Actor Transform
- **場所**: スポーンポイントアクターから `Get Actor Transform` を検索
- **戻り値**: Transform構造体（位置、回転、スケール）

#### Spawn Actor from Class
- **場所**: 右クリック → `Spawn Actor from Class` を検索
- **Class**: `BP_AIBlueSide` または `BP_AIRedSide`
- **Transform**: スポーンポイントのTransform
- **戻り値**: スポーンされたAIアクター

### 実装例（完全版）

```
[Custom Event SpawnAITeams]
    ↓
[Get All Actors of Class] → Class: BP_BlueSideSpownPoint
    ↓
[Branch] → Condition: [Array Length] > 0
    ├─ True →
    │   [Set BlueSideSpawnPoints] → [Return Value]
    │   ↓
    │   [For Loop] → First: 0, Last: [AICountPerTeam] - 1
    │   │   ↓
    │   │   [Array Length] → Array: BlueSideSpawnPoints
    │   │   ↓
    │   │   [Integer % Integer] → [Loop Index] % [Array Length]
    │   │   ↓
    │   │   [Array Get] → Array: BlueSideSpawnPoints, Index: [Result]
    │   │   ↓
    │   │   [Get Actor Transform]
    │   │   ↓
    │   │   [Spawn Actor from Class]
    │   │       - Class: BP_AIBlueSide
    │   │       - Transform: [Return Value]
    │   │   ↓
    │   │   [Print String] → "Spawned BlueSide AI" (デバッグ用)
    │   └─ False →
    │       [Print String] → "No BlueSide Spawn Points Found!" (エラー)
    ↓
[Get All Actors of Class] → Class: BP_RedSideSpawnPoint
    ↓
[Branch] → Condition: [Array Length] > 0
    ├─ True →
    │   [Set RedSideSpawnPoints] → [Return Value]
    │   ↓
    │   [For Loop] → First: 0, Last: [AICountPerTeam] - 1
    │   │   ↓
    │   │   [Array Length] → Array: RedSideSpawnPoints
    │   │   ↓
    │   │   [Integer % Integer] → [Loop Index] % [Array Length]
    │   │   ↓
    │   │   [Array Get] → Array: RedSideSpawnPoints, Index: [Result]
    │   │   ↓
    │   │   [Get Actor Transform]
    │   │   ↓
    │   │   [Spawn Actor from Class]
    │   │       - Class: BP_AIRedSide
    │   │       - Transform: [Return Value]
    │   │   ↓
    │   │   [Print String] → "Spawned RedSide AI" (デバッグ用)
    └─ False →
        [Print String] → "No RedSide Spawn Points Found!" (エラー)
```

### 注意事項

1. **スポーンポイントの配置**: レベルに十分な数のスポーンポイントを配置してください
2. **配列の空チェック**: スポーンポイントが見つからない場合のエラーハンドリングを追加
3. **重複スポーン**: 同じ位置に複数のAIがスポーンしないように注意
4. **ナビゲーションメッシュ**: AIが正常に動作するためには、NavMeshが生成されている必要があります

---

## 3. プレイヤーコントローラー（PC_AcceleraXion）の実装

### 変数の追加

| 変数名 | 型 | デフォルト値 |
|--------|-----|-------------|
| MainMenuWidget | Widget (Object Reference) | None |
| ResultWidget | Widget (Object Reference) | None |

### BeginPlayイベントの実装

```
[Event BeginPlay]
    ↓
[Create Widget] → Class: WB_Main
    ↓
[Set MainMenuWidget] → [Return Value]
    ↓
[Add to Viewport] → [MainMenuWidget]
    ↓
[Set Input Mode UI Only]
    ↓
[Set Show Mouse Cursor] → Show Mouse Cursor: True
```

### StartGame関数（カスタムイベント）の実装

```
[Custom Event StartGame]
    ↓
[Remove from Parent] → [MainMenuWidget]
    ↓
[Set Input Mode Game Only]
    ↓
[Set Show Mouse Cursor] → Show Mouse Cursor: False
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Call StartGame]
```

### ShowResultScreen関数（カスタムイベント）の実装

```
[Custom Event ShowResultScreen]
    ↓
[Create Widget] → Class: WB_Result
    ↓
[Set ResultWidget] → [Return Value]
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Get PlayerKills]
    ↓
[Get PlayerDeaths]
    ↓
[Call SetResultData] → Kills: [PlayerKills], Deaths: [PlayerDeaths]
    ↓
[Add to Viewport] → [ResultWidget]
    ↓
[Set Input Mode UI Only]
    ↓
[Set Show Mouse Cursor] → Show Mouse Cursor: True
```

---

## 4. メインメニューUI（WB_Main）の実装

### ウィジェットのデザイン

1. **Canvas Panel** をルートとして使用
2. **Start Button** を追加
3. 必要に応じてタイトルテキストを追加

### StartボタンのOnClickedイベント

```
[Button OnClicked]
    ↓
[Get Player Controller]
    ↓
[Cast to PC_AcceleraXion]
    ↓
[Call StartGame]
```

**または、ゲームモードを直接呼び出す場合:**

```
[Button OnClicked]
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Call StartGame]
```

---

## 5. リザルト画面UI（WB_Result）の作成

### ウィジェットのデザイン

1. **Canvas Panel** をルートとして使用
2. **Text** を追加（タイトル「リザルト」）
3. **Text** を追加（キル数表示用）
4. **Text** を追加（デス数表示用）
5. **Text** を追加（K/D比表示用）
6. **Button** を追加（再プレイボタン）
7. **Button** を追加（終了ボタン）

### 変数の追加

| 変数名 | 型 | デフォルト値 |
|--------|-----|-------------|
| Kills | Integer | 0 |
| Deaths | Integer | 0 |
| KillsText | Text (Object Reference) | None |
| DeathsText | Text (Object Reference) | None |
| KDText | Text (Object Reference) | None |

### SetResultData関数（カスタムイベント）の実装

**パラメータ:**
- `Kills` (Integer)
- `Deaths` (Integer)

**実装内容:**
```
[Custom Event SetResultData]
    Parameters: Kills (Integer), Deaths (Integer)
    ↓
[Set Kills] → [Kills]
    ↓
[Set Deaths] → [Deaths]
    ↓
[Set Text] → [KillsText], Text: "キル数: " + [ToString] ([Kills])
    ↓
[Set Text] → [DeathsText], Text: "デス数: " + [ToString] ([Deaths])
    ↓
[Branch] → Condition: [Deaths] > 0
    ├─ True →
    │   [Float / Float] → [Kills] / [Deaths]
    │   ↓
    │   [Set Text] → [KDText], Text: "K/D: " + [ToString] ([Result])
    └─ False →
        [Set Text] → [KDText], Text: "K/D: " + [ToString] ([Kills])
```

### 再プレイボタンのOnClickedイベント

```
[Button OnClicked]
    ↓
[Get World]
    ↓
[Get Name]
    ↓
[Open Level] → Level Name: [Return Value]
```

### 終了ボタンのOnClickedイベント

```
[Button OnClicked]
    ↓
[Quit Game]
```

---

## 6. プレイヤー（BP_PlayerBase）の実装

### 死亡時の処理（既存のOnDeathイベントに追加）

```
[OnDeath Event] (既存の処理)
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Call OnPlayerKilled]
```

### 敵を倒した時の処理

```
[OnKillEnemy Event] (既存の処理)
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Call OnEnemyKilled]
```

---

## 7. AI（BP_AIBase）の実装

### 死亡時の処理（既存のOnDeathイベントに追加）

```
[OnDeath Event] (既存の処理)
    ↓
[Get Game Mode]
    ↓
[Cast to GM_AcceleraXion]
    ↓
[Call OnEnemyKilled]
```

---

## 重要なノードの説明

### Get Game Mode
- 現在のゲームモードを取得するノード
- 場所: `Get Game Mode` を検索

### Cast to [Class]
- オブジェクトを特定のクラスにキャストするノード
- 失敗した場合は実行されない

### Set Timer by Function Name
- 指定した関数を一定時間後に呼び出すノード
- Looping を True にすると繰り返し実行される

### Get All Actors of Class
- 指定したクラスのすべてのアクターを取得するノード
- 配列として返される

### Open Level
- 指定したレベルを開くノード
- レベル名は文字列で指定

### Quit Game
- ゲームを終了するノード

---

## トラブルシューティング

### タイマーが動作しない
- 関数名が正確に一致しているか確認
- タイマーをクリアする前に再度設定していないか確認

### UIが表示されない
- ウィジェットが Viewport に追加されているか確認
- Input Mode が UI Only になっているか確認

### ゲームモードの関数が呼ばれない
- Cast が成功しているか確認（Cast ノードの出力ピンが接続されているか）
- 関数が Public になっているか確認

### スポーンが動作しない
- PlayerStart が有効になっているか確認
- ゲームモードで正しい Pawn クラスが設定されているか確認
- スポーンポイントがレベルに配置されているか確認
- AIクラス（BP_AIBlueSide, BP_AIRedSide）が正しく指定されているか確認

### AIがスポーンしない
- スポーンポイント（BP_BlueSideSpownPoint, BP_RedSideSpawnPoint）がレベルに配置されているか確認
- Spawn Actor from Class ノードで正しいクラスを指定しているか確認
- スポーンポイントの配列が空でないか確認（Print String でデバッグ）

